# Test
This is a test repo.

It is basically there to test other tools, so any code in here is simply dummy.

Such tools might include CI systems, so the code in here may well contain things in an various programming languages that would break CI checks.
